import {<%= entityName %> } from '../entities/<%= entityName.toLowerCase() %>'.entity;
import {<%= entityName %>Dto } from '../dto/<%= entityName.toLowerCase() %>'.dto;

export class Create<%= entityName %>Command {
    constructor(
        public readonly <%= entityName %> }Dto%>: <%= entityName %>,
    ){}
}